version: '3.8'

services:
  bot:
    build: .
    container_name: linkedin_bot
    volumes:
      - .:/app
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite:////app/data/posts.db
    restart: unless-stopped
